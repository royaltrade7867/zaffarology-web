node_modules/
.next/
out/
build/
.DS_Store
*.pem

# Local env — the API URL points at the production backend.
# `.env` itself is ignored too: it is real deployment config, and it was only
# ever untracked by luck rather than by rule.
.env
.env*.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

next-env.d.ts
*.tsbuildinfo
